function solve(input) {
    let dashboard = [[false, false, false],
    [false, false, false],
    [false, false, false]]

    const winingMoves = []

    console.log(dashboard.map(e => e.join('\t')).join('\n'))
}


solve()